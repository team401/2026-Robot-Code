package frc.robot.auto;

// Mostly just a wrapper for a AutoAction to make it easier to load from JSON.
public class Routine {
  // This is so that we can have the routine have a custom name
  String name = null;
  AutoAction routine;
}
